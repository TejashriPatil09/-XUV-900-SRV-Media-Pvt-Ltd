// Add dropdown functionality (optional)
document.querySelectorAll('.nav-links a').forEach(link => {
    link.addEventListener('click', () => {
        // Implement dropdown toggle here
    });
});
